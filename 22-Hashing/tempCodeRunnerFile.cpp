map<int, int>::iterator it;
    // for (it = m.begin(); it != m.end(); it++)
    // {
    //     int x = it->ss, y = it->ff;
    //     if (y != 0)
    //         ans += x * (x - 1) / 2;
    //     else if (y == 0)
    //         ans += x;
    // }